import React, { } from 'react';
import s from './Settings.module.css';
console.log(s);
const Settings = () => {
    return  (
        <div>
          settings
        </div>
    )
        
}

export default Settings;